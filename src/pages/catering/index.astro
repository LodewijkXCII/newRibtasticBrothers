---
import CateringParters from "../../components/CateringPartners.astro";
// import PartnerCarousel from "../../components/PartnerCarousel.vue";

import BaseHeading from "../../layouts/BaseHeading.astro";

import HeaderImage from "../../components/HeaderImage.astro";
import NavBar from "../../layouts/NavBar.astro";
import Footer from "../../layouts/Footer.astro";

import bbqSmokerImage from "../../assets/img/jon-tyson-kctt4tL1dkE-unsplash.jpg";
import foodtruckImage from "../../assets/img/matt-nelson-cTyVIvzQslE-unsplash.jpg";
import eventImage from "../../assets/img/samantha-gades-fIHozNWfcvs-unsplash.jpg";

const title = "Catering - The Ribtastic Brothers";
let heading = {
    title: 'Sensatie',
    subtitle: 'Met bakfiets of smoker',
}

let headerURL =
  "https://ribtastic-brothers.s3.eu-west-2.amazonaws.com/static/ribtastic_brothers_catering+(1).jpg";

const cateringOptions = "https://admin.ribtasticbrothers.nl/catering-opties";
const cateringMogelijkheden = [
  {
    name: "BBQ Smoker",
    image: bbqSmokerImage,
  },
  {
    name: "Foodtruck",
    image: foodtruckImage,
  },
  {
    name: "Event",
    image: eventImage,
  },
];

const response = await fetch(cateringOptions);

const data = await response.json();
const catering = data.map((option) => {
  return {
    id: option.id,
    name: option.titel,
    price: option.prijs.toLocaleString(),
    cateringProducts: option.catering_productens,
    recommended: option.aanbevolen,
    slug: option.slug,
  };
});
---

<html lang="nl">
  <head>
    <BaseHeading title={title} />
  </head>

  <body>
    <NavBar/>
    <HeaderImage image={headerURL} header={heading}/>

    <section class="grid-4">
      <div class="text" style="margin: auto;">
        <h1>Catering mogelijkheden</h1>
        <h2>Echt niet alleen BBQ</h2>
      </div>
      {
        cateringMogelijkheden.map((d) => (
          <section class="imageCard">
            <img src={d.image} alt="" class="imageCard--image" />
            <div class="imageCard--title">
              <h2 class="imageCard--text">{d.name}</h2>
            </div>
          </section>
        ))
      }
    </section>

    <div class="container">
      <div class="textInfo">
        <p>
          Naast het bieden van lekkere BBQ versnaperingen op zonnige dagen aan
          het strand, zijn we tevens gespecialiseerd in het verzorgen van
          catering op evenementen. Of het nu een lunch, een bruiloft, een
          verjaardag, een festival of een bedrijfsfeest is, laat ons uw
          genodigden verrassen met de heerlijke smaak van onze BBQ creaties.
        </p>
        <p>
          Ieder evenement is uniek en vraagt om zijn eigen invulling en
          aankleding. Wij gaan voor de perfecte match en passen onze BBQ
          specialiteiten/diensten daarom graag aan op de exacte wensen van het
          publiek. Wij voelen de verantwoordelijkheid om uw feest een succes te
          maken en denken graag actief met u mee. We staan open voor verzoeken
          en zullen deze uitgebreid in behandeling nemen.
        </p>
      </div>
    </div>

    <CateringParters />
    <!-- <PartnerCarousel client:load /> -->

    <Footer />
  </body>
</html>
